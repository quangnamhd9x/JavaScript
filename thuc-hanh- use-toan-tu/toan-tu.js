let inputWidth;
let inputHeight;

inputWidth = prompt("Nhap chieu rong:")
innerHeight = prompt("Nhap chieu dai")

let width = parseInt(inputWidth);
let height = parseInt(innerHeight);
let area = width * height;

alert("Dien tich la:" + area);
